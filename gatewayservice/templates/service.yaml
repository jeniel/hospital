apiVersion: v1
kind: Service
metadata:
  name: {{ include "gatewayservice.fullname" . }}
  labels:
    {{- include "gatewayservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gatewayservice.selectorLabels" . | nindent 4 }}
