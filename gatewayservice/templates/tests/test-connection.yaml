apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gatewayservice.fullname" . }}-test-connection"
  labels:
    {{- include "gatewayservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gatewayservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
