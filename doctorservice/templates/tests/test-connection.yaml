apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "doctorservice.fullname" . }}-test-connection"
  labels:
    {{- include "doctorservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "doctorservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
