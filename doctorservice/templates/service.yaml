apiVersion: v1
kind: Service
metadata:
  name: {{ include "doctorservice.fullname" . }}
  labels:
    {{- include "doctorservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "doctorservice.selectorLabels" . | nindent 4 }}
