namespace: appointment
replicaCount: 1
appName: appointment
image:
  repository: jeniel/appointment

  pullPolicy: IfNotPresent

  tag: "1.0.0"

imagePullSecrets: []

service:

  type: ClusterIP
  targetPort: 8083
  port: 80

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: hospital.192.168.49.2.nip.io
           
  tls: []
 
